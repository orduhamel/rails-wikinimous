<h1><%= @article.title %></h1>


<p><%= @article.content %></p>

<%= link_to 'edit', edit_article_path(@article), class: 'btn' %>
<%= link_to 'delete', article_path(@article), class: 'btn', method: 'delete', data: { confirm: "Are you sure you want to delete the article #{@article.title}?" } %>

<%= link_to 'Back to list', articles_path %>
